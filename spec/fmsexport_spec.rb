require "spec_helper"

describe Fmsexport do
  it "has a version number" do
    expect(Fmsexport::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
